# ============================================================
# Config 层组件: 集中存放所有配置项(魔法数/引脚/错误码/公共类型)
# 仅含头文件无源文件, 是全工程最底层依赖
# ============================================================
idf_component_register(
    INCLUDE_DIRS "include"
)
