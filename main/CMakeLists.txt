# ============================================================
# main 组件: 系统启动入口
# 仅调用 App 层启动函数, 不含业务逻辑
# ============================================================
idf_component_register(
    SRCS "main.c"
    INCLUDE_DIRS "."
    REQUIRES bms_app
)
